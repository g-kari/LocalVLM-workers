name = "local-vlm-workers"
compatibility_date = "2025-04-01"
workers_dev = true
preview_urls = false

# Cloudflare Workers で SPA配信 + API Proxy
# デプロイはGitHub接続による自動デプロイ
# カスタムドメイン: LocalVLM-workers.0g0.xyz

[assets]
directory = "./dist"

[vars]
# 環境変数（シークレットはCloudflare Dashboardで設定）
